import { AUTH_TO_SERVER } from "../types/authType";

const initialState = {

}

export default authReducer = (state = initialState, action) => {
    switch (action.type) {
        case AUTH_TO_SERVER:
        
            break;
    
        default:
            return state;
    }
}